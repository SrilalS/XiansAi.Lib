# Copy this file to .env and fill in your values:
#   cp env.template .env

XIANS_SERVER_URL=http://localhost:5005

# Agent certificate (Base64-encoded X.509) used by XiansPlatform.InitializeAsync
XIANS_API_KEY=

# Bearer token for the Xians Admin API (/api/v1/admin/...).
# Accepts either the raw token or a value already prefixed with "Bearer ".
XIANS_ADMIN_TOKEN=
